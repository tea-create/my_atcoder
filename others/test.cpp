#include <stdio.h>
#include "atcoder/internal_bit.hpp"

signed main(void){
	char c = 0;
	while((c=getchar()) != '\n'){
		putc(c, stdout);
		putc('\n', stdout);
	}
		printf("test is end.");
	return 0;
}