#include <bits/stdc++.h>

// #define int long long
#define endl '\n'

using namespace std;

#define rep(i,r,l) for(auto i = r;i<l;i++)

vector<int> a;
int n,k;

signed main() {
    cin >> n;
	cin >> k;
	rep(i,0,n){
		cin >> a[i];
	}
    int res = 0;
    for (int bit = 0; bit < (1 << N); ++bit) {
		rep(i,0,n){
			if(a[i])
		}
    }

    cout << res << endl;
}